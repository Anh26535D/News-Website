apiVersion: v1
kind: Service
metadata:
  name: fe-service
  namespace: npm-fe
spec:
  selector:
    app: fe-image
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer